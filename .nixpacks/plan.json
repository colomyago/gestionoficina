{
  "providers": ["php"],
  "buildCommand": "composer install --optimize-autoloader --no-dev --no-interaction --ignore-platform-reqs && npm ci --legacy-peer-deps && npm run build",
  "startCommand": "bash railway-deploy.sh && php artisan serve --host=0.0.0.0 --port=$PORT",
  "staticAssets": {
    "public": "/"
  }
}
